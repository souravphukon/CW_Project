<?xml version="1.0" encoding="UTF-8"?>

<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item
        android:id="@android:id/background">
        <shape>
            <corners
                android:radius="5dip" />
            <gradient
                android:startColor="@color/grey_light"
                android:centerColor="@color/grey_light"
                android:centerY="0.50"
                android:endColor="@color/grey_light"
                android:angle="270" />
        </shape>
    </item>
    <item
        android:id="@android:id/progress">
        <clip>
            <shape>
                <corners
                    android:radius="5dip" />
                <gradient
                    android:startColor="@color/sub_title_color"
                    android:endColor="@color/sub_title_color"
                    android:angle="90" />
            </shape>
        </clip>
    </item>
</layer-list>